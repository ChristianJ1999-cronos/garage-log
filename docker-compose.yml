services:
  db:
    image: postgres:16
    container_name: garage_log_db
    environment:
      POSTGRES_USER: garagelog
      POSTGRES_PASSWORD: garagelog
      POSTGRES_DB: garagelog
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: